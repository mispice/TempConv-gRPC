steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-f', 'Dockerfile.frontend', '-t', 'gcr.io/$PROJECT_ID/tempconv-frontend:latest', '.']
images:
- 'gcr.io/$PROJECT_ID/tempconv-frontend:latest'
